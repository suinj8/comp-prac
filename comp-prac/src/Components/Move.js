import * as React from "react";
import { AppBar, Grid } from "@mui/material";

const Move = () => {
  return (
    <AppBar position="static">
      <Grid></Grid>
    </AppBar>
  );
};

export default Move;
